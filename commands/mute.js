const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
message.reply("This command has not been developed yet. It will be implemented soon.")

    }
    module.exports.help = {
        name: "mute"
      }